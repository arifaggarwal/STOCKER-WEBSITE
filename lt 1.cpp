#include<iostream>   //header files between <> and "" 
using namespace std;  //logical region in which we define functions
int main() 
{
    cout<<"hello";  //(printf was a function) in c lang but cout is an object << is insertion operater 
    return 0;
}
